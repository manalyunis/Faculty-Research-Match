# Faculty Research Match - Production Environment Variables
# Optimized for Render.com deployment with Transformers.js + PostgreSQL

# Application Configuration
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1

# PostgreSQL Database (REQUIRED)
# Render automatically provides this as DATABASE_URL when you link a PostgreSQL service
# You don't need to set this manually - Render will inject it
# DATABASE_URL=postgresql://user:password@host:port/database

# Transformers.js Configuration
TRANSFORMERS_CACHE=/app/.cache
TRANSFORMERS_OFFLINE=false

# OpenAI Configuration (OPTIONAL - only if using OpenAI features)
OPENAI_API_KEY=your_openai_key_here

# Feature Flags
ENABLE_ADVANCED_SIMILARITY=true
ENABLE_CACHING=true

# Domain Configuration (update with your Render URL after deployment)
NEXT_PUBLIC_APP_URL=https://your-app-name.onrender.com

# Logging
LOG_LEVEL=info

# Performance
MAX_CONCURRENT_REQUESTS=50
REQUEST_TIMEOUT=30000  # 30 seconds

# Deployment Notes:
# 1. DATABASE_URL is automatically set by Render when you add PostgreSQL
# 2. Run database/schema.sql on your Render PostgreSQL after creation
# 3. Import your faculty data using /api/ingest or bulk insert
# 4. Generate embeddings using /api/generate-embeddings
# 5. Update NEXT_PUBLIC_APP_URL after first deployment
